#include <stdio.h>
int main(void)  
{
	int a[10],temp;
	clrscr();
	int i;
	for(i=1;i<10;i++)
	{
	 scanf(%d,7a[i]);
	 }
	  temp[0];
	  for(i=1;i<10;i++)
	  {
	  	if(a[0]<a[i])
	        {
	        	temp a[i];
	        	
	        }
	return 0;
	  }
}
#include <stdio.h>
int main(void)  
{
	int a[10],temp;
	clrscr();
	int i;
	for(i=1;i<10;i++)
	{
	 scanf(%d,7a[i]);
	 }
	  temp[0];
	  for(i=1;i<10;i++)
	  {
	  	if(a[0]<a[i])
	        {
	        	temp a[i];
	        	
	        }
		  printf(" the largest number is %d",temp);
			
	return 0;
	  }
}
